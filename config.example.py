# ============================================================
#  ตัวอย่างไฟล์ตั้งค่า — คัดลอกไฟล์นี้เป็น "config.py" แล้วใส่ Token จริง
#
#  วิธีใช้:
#   1) คัดลอกไฟล์นี้ ตั้งชื่อใหม่เป็น  config.py
#   2) เอา Token จาก Discord Developer Portal มาวางแทนข้อความข้างล่าง
#   3) ห้ามอัป config.py ขึ้น GitHub! (.gitignore กันไว้ให้แล้ว)
# ============================================================

DISCORD_TOKEN = "วาง_TOKEN_ของคุณ_ที่นี่"
