# ============================================================
#  ตัวอย่างไฟล์ตั้งค่า — คัดลอกไฟล์นี้เป็น "config.py" แล้วใส่ค่าจริง
#
#  วิธีใช้:
#   1) คัดลอกไฟล์นี้ ตั้งชื่อใหม่เป็น  config.py
#   2) ใส่ค่า Token จริงแทนข้อความตัวอย่าง
#   3) ห้ามอัป config.py ขึ้น GitHub! (.gitignore กันไว้ให้แล้ว)
# ============================================================

# Token ของบอทจาก Discord Developer Portal
DISCORD_TOKEN = "วาง_TOKEN_ของคุณ_ที่นี่"

# Token ของกรมอุตุนิยมวิทยา (TMD) — ขอที่ https://data.tmd.go.th
TMD_TOKEN = "วาง_TMD_TOKEN_ที่นี่"
