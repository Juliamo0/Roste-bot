# Changelog

รูปแบบตาม [Keep a Changelog](https://keepachangelog.com/th/1.1.0/)
เลขเวอร์ชันตาม [Semantic Versioning](https://semver.org/lang/th/)

> **หมายเหตุเรื่องเลขเวอร์ชัน:** `0.y.z` แปลว่ายังอยู่ในช่วงที่เปลี่ยนโครงสร้าง/พฤติกรรมได้
> โดยไม่ถือเป็น breaking change — API ภายในยังไม่ผูกมัด `-alpha` ย้ำอีกชั้นว่ายังไม่พร้อม
> ให้พึ่งพาในงานจริง

---

## [0.1.0-alpha] — 2026-08-09

เวอร์ชันแรกที่ติดเลขอย่างเป็นทางการ ก่อนหน้านี้พัฒนาแบบไม่มีเวอร์ชันมาตลอด
รายการด้านล่างคือสถานะ ณ จุดนี้ ไม่ใช่ของใหม่ทั้งหมดในรอบเดียว

### ระบบสนทนา
- คุยผ่าน Discord (mention ในเซิร์ฟเวอร์ / DM) ด้วย LLM ท้องถิ่นผ่าน Ollama
- **ความจำระยะยาว** — สรุปบทสนทนาเก็บเป็น fact รายคน พร้อมระบบ supersede
  (ข้อมูลใหม่ทับข้อมูลเก่าในหมวดเดียวกัน แต่ไม่ลบทิ้ง กู้คืนได้ถ้าสรุปผิด)
- **vector memory** — ค้นความจำด้วย ChromaDB + สรุปเนื้อหา PDF
- **tool calling** — อากาศ (TMD/Open-Meteo), ราคาน้ำมัน, เวลา, ค้นสถานที่, ค้นเว็บ,
  ไฟดับ, สั่งพิมพ์เอกสาร พร้อม fail-safe ครบทุกชั้น (tool หลอน/พารามิเตอร์ขาด/handler พัง)
- **guard คำตอบ** — กันหลุดคาแร็กเตอร์ (ตอบอังกฤษล้วน / อ้างว่าเป็น AI / สรรพนามผิดเพศ)
- **ตัวละครเป็น template** — `persona.py` มีรอสเต้มาให้ครบเป็นตัวอย่างที่ใช้งานได้จริง
  แก้เป็นตัวละครของตัวเองได้ทันที (คอมเมนต์ในไฟล์อธิบายกับดักที่เจอมาแล้ว)

### ระบบเสียง
- **TTS ภาษาไทย** ด้วย F5-TTS-THAI v2 (โคลนเสียงจาก reference audio, local ล้วน)
- **sentence streaming** — เล่นทีละ segment ทันทีที่เจนเสร็จ ไม่รอทั้งคำตอบ
  (เสียงแรกออกใน ~3s แทน ~15-20s) พร้อม prefetch กลบช่องว่างระหว่าง segment
- **per-segment fail-safe** — แต่ละ segment มี chain ของตัวเอง:
  F5 (retry 1 ครั้ง) → edge-tts → ข้าม segment นั้น ไม่ทำให้ทั้งคำตอบเงียบ
- **preprocess ข้อความก่อนอ่าน** — ตัวเลข/ปี พ.ศ./°C/หน่วย/รหัสน้ำมัน → คำอ่านไทย,
  ขยาย ๆ ตามพยางค์ท้าย
- **fade รอยต่อ segment** — edge fade 15ms กันเสียงคลิกตรงรอยต่อ
- **karaoke** — ร้องเพลง cover ด้วยเสียง RVC จากโฟลเดอร์เพลงในเครื่อง

### โครงสร้าง / ความปลอดภัย
- F5 และ RVC รันใน subprocess แยก (`f5_venv`, `rvc_venv`) กัน dependency ชนกับบอทหลัก
- **worker hang timeout** — kill subprocess ที่ค้างใน 60 วิ กัน lock ค้างถาวร
- **singleton lock** — กันเปิดบอทซ้อนกันหลายตัว
- rate limiting (3 วิ/คน), guild allowlist, DM allowlist, SerpAPI quota guard
- **582 tests** + CI บน GitHub Actions (Windows)

### ที่ไม่รวมมาใน repo (ต้องเตรียมเอง)
- `.env` — Discord token + API keys (ก๊อปจาก `.env.example`)
- **โมเดลเสียง RVC** และ **reference audio** — เป็นเสียงส่วนตัว ไม่แจกจ่าย
- `f5_venv/` และ `rvc_venv/` — ต้องสร้างเองทั้งคู่ (ดูขั้นตอนใน README)
- ไฟล์เพลง karaoke — ลิขสิทธิ์ของเจ้าของเพลง

### ข้อจำกัดที่รู้อยู่
- **Windows เท่านั้น** — ใช้ `msvcrt` (singleton lock) และ `pywin32` (สั่งพิมพ์)
- ต้องมี GPU สำหรับ F5/RVC (CPU ช้าจนใช้งานจริงไม่ได้)
- ต้องมี Ollama รันอยู่ในเครื่อง
- **คำภาษาอังกฤษยังไม่ทับศัพท์** ก่อนส่งเข้า TTS — `th_to_g2p` แปลงเฉพาะอักษรไทย
  คำอังกฤษหลุดไปหาโมเดลดิบๆ ทำให้อ่านเพี้ยนได้
- โทน/จังหวะเสียงกระโดดตรงรอยต่อ segment — แต่ละก้อนเจนแยกกันโดยไม่รู้บริบทก้อนก่อนหน้า
  (edge fade แก้ได้แค่เสียงคลิก ไม่ได้แก้เรื่องนี้)
