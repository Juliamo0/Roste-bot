# รอสเต้ (Roste) — Python dependencies (venv หลักที่รัน bot.py)
# ติดตั้ง: pip install -r requirements.txt
# (ไม่รวม torch/f5-tts-th/rvc-python — อยู่ใน f5_venv/rvc_venv แยกต่างหาก ดู README.md)

discord.py==2.7.1
aiohttp==3.14.1
ddgs==9.14.4
pypdf==6.14.2
pywin32==312
PyNaCl==1.5.0
chromadb==1.5.9
python-dotenv==1.2.2

# sentence-streaming TTS — ตัดประโยคไทย (crfcut) + เสียงสำรอง
pythainlp==5.3.4
python-crfsuite==0.9.12
soundfile==0.14.0
edge-tts==7.2.8
